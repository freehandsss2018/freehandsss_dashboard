# /execute — 執行已確認計畫

**依賴**：用戶當前授權、適用的 `.fhs/ai/AGENTS.md`；若源自 `/cl-flow`，需 `artifacts/{flow_id}/cl-final-plan.md` 與 `state.json`。
**不可攜平台**：無；若計畫引用特定服務，先依計畫確認該服務可用。

1. 確認用戶要求執行的具體範圍；若來自 `/rp`，保留原始授權邊界。若有 flow，核對最終計畫、裁決與鎖定狀態；計畫沒有覆蓋的工作不得順帶執行。
2. 列出將修改的檔案及預期驗收。架構改動依專案憲法的批准規則處理；其餘已授權工作直接進行。
3. 按計畫逐步實作。每步檢查真實檔案與工具輸出；遇阻斷或前提失效，停止受影響步驟，記錄證據並更新方案。
4. 跑與改動相稱的測試或實際驗證。由獨立審查要求覆蓋的項目，提交可重現證據；不可用「已寫代碼」代替驗收。
5. 檢查後效同步：新增或移動檔案更新 repo map；治理或流程改動更新完成記錄；對外行為改動更新 CHANGELOG；專案特有規則按 AGENTS 路由更新。
6. 回報：**已完成**、**點運作**、**點維護**；附測試結果、未完成事項與實際改動清單。Git 提交由 `/commit` 處理，除非用戶明確要求同輪提交。
