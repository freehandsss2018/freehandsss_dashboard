# 03 — 判斷力外化（Judgment Rubrics）

> **Version**: v1.0.0（2026-07-04，Session 137）
> **這是什麼**：高階模型靠直覺做對的五類判斷，改寫成任何等級模型可機械執行的判準。每條附一個真實正例（FHS session 史）與一個反例。
> **怎麼用**：卡在「該不該升級 / 算不算完成 / 要不要問人 / 是不是方向錯了」時，翻對應節，照判準走，不靠感覺。

---

## R1 — 何時該升級模型

**判準（任一成立即升級，帶完整失敗軌跡）**：
1. 同一子任務兩輪修復後驗收仍 FAIL（機械觸發，見 02 §4）。
2. 問題跨 ≥3 個模組且因果鏈不明（症狀在 A，改了 B，冒出 C）。
3. 需要在**互相衝突的既有規則**之間裁決（不是查漏規則，是規則打架）。
4. 任務要求「取捨」而非「執行」——多個方案各有代價，且代價不可通約（如 token 成本 vs 隱私風險）。

**不升級（先做完這些再說）**：Edit old_string 不匹配（→先 Grep 看實際內容）；工具報錯（→讀錯誤訊息照做）；查得到的資料缺失（→查 learnings/handoff/live DB）。

- ✅ **正例**：S133 tg2 節點 invalid syntax，兩輪修 payload 無效後，root-cause 追到 emoji surrogate pair 序列化——這種「症狀離根因三層遠」的題，第二輪失敗時就該升級或換診斷路徑，而不是第三次改 payload。
- ❌ **反例**：第一次 PUT n8n workflow 收到 400 就升級——先查 learnings（PUT body 只能含 4 欄）是已記載 pitfall，30 秒解決。**升級前必查 learnings.md 與 handoff 便攜塊「易猜錯」欄。**

---

## R2 — 何時算「真的完成」

**判準：完成 = 每條驗收條件都有一份客觀證據。** 證據類型分級：
1. **運行證據**（最強）：HTTP 狀態碼、測試輸出、實際執行 log、live SQL 回傳值。
2. **read-back 證據**：fresh context 讀回檔案確認不變量成立。
3. **計數證據**：grep -c 改前改後數字。
4. ~~「我讀了一遍覺得對」~~——**不是證據**。

Bug 修復另需過 `fhs-bug-triage` skill 的 5-Gate。財務/schema/n8n/生產 HTML 的驗收分流表在 `02 §5`（驗證不自驗）。

**宣告完成的回報格式**：逐條列驗收條件 → 各附證據（引用輸出原文或檔案:行號）。缺證據的條目寫「未驗證」，不寫「應該沒問題」。

- ✅ **正例**：S127 Write Alerts 修復——不止改了 contentType，還做 mock POST 實測 HTTP 201 + DELETE 清理，才宣告完成。「部署成功」≠「功能正確」，201 才是證據。
- ❌ **反例**：「我已將函式改為 X，bug 應已修復」——零運行證據。FHS 史上這句話後面跟著的是 S103「核對帳單捷徑從未真正生效」（寫了、上線了、從沒工作過，直到 S109 才發現）。

---

## R3 — 何時該停下來問 Fat Mo

**必問（任一成立，先停）**：
1. 動作**不可逆且未獲本次授權**：覆蓋 `current.html`、DROP/DELETE 生產數據、對外發送（Telegram/email/publish）、git push force。
2. **財務規則語義存在兩種讀法**且金額會因此不同（FHS 鐵律：財務算錯=嚴重核心錯誤，禁止猜）。
3. 發現的新事實**與 handoff 已定決策矛盾**（決策可能基於過時前提——指出矛盾，讓 Fat Mo 裁決，不擅自推翻也不裝沒看見）。
4. 任務要求與憲法硬規則衝突（如任務暗示要改 HTML ID）。

**必不問（自己查，問了算違規）**：可以從 Supabase live 查到的資料、learnings/handoff 已記載的答案、可以用一次實測消歧的技術問題。既有 memory 鐵律：嚴禁直接問可自查/自析的事；決策題要給優劣對比+建議再問。

**問的格式**：一次問完一批（≤5 題）、每題給選項與建議、標注你建議哪個和為什麼。零散連環問是失職。

- ✅ **正例**：S112 查證後發現 Fat Mo 的假設「物料改 115，base 就該 ≈115」是誤讀（185=組裝成本含繪圖+環扣）——**直接指出用戶假設錯了**，而不是照假設去「修」正確的數據。
- ❌ **反例**：問 Fat Mo「訂單編號是什麼格式？」——S116 的正確做法是 live SELECT 31 筆真實訂單直接校準出 regex。可查的事拿去問人，浪費的是決策者的時間。

---

## R4 — 方向錯了的訊號（換路，不是重試）

**判準（任一成立 → 回到假設層重新規劃，禁止第三次重試）**：
1. **兩輪修復修掉不同症狀，但同一根因換個地方又冒出來**（打地鼠模式）。
2. **每次修復都引入新的破壞**（改動面比問題面大 = 手術位置錯了）。
3. **核心假設被 live 數據直接證偽**（不是「效果不好」，是「前提不成立」）。
4. 方案需要越來越多特例補丁才能自圓其說（第 3 個 if-特例 = 抽象錯了）。

**換路的動作**：停手 → 一段話寫下「原假設是什麼、哪個證據否定了它」→ 列 2-3 條新路 + 建議 → 視風險等級自行擇路或升 Fat Mo（R3）。把證偽記錄寫進當前任務的 artifacts，防下個 session 重走死路。

- ✅ **正例**：S111 實測發現 Meta 匯出非 ZIP、Drive Trigger 對深層資料夾不觸發——v1 架構**整個棄用重建 v2**，而不是給 v1 加補丁。判斷點：被證偽的是「觸發機制存在」這個前提，前提死了方案就死了。
- ❌ **反例**：curl 成功、Node https 失敗 → 「換路重寫整個模組」。錯。這是已記載的 Cloudflare 指紋 pitfall（learnings），正解是改用 curl 子程序——**先查 learnings 再判斷是方向問題還是已知坑**。R4 的前置永遠是 R1 的「必查清單」。

---

## R5 — 品質底線怎麼驗（分域速查）

| 域 | 底線驗法 | 依據/工具 |
|---|---|---|
| 視覺/版面 | playwright / preview 實測 computed style，**禁純讀碼臆測** | 既有鐵律（feedback_visual_bug）|
| 財務數字 | live SQL 數學驗算，附具體訂單號；對照 Finance Bible 規則 | finance-gatekeeper skill |
| n8n 部署 | GET 回讀 versionId 變更 + 節點數 + credential 完整性；行為改動加 mock 實測 | S127/S136 慣例 |
| Supabase migration | smoke test 斷言（預期行數/值），首跑異常必查證不硬過 | S112/S130 慣例 |
| 文件/制度 | read-back + 內部路徑存在性檢查（引用的每個檔案路徑實際存在） | 02 §5 |
| 巨檔 HTML | 替換三步計數 + 受影響函式 node 抽出 smoke test（S124 慣例） | 02 §6 |

---

## R6 — 誠實極限（harness 補不了的，怎麼辦）

**拆解、驗證、多樣本評審能補執行品質；以下兩類補不了，遇到照規則辦，不硬撐**：

1. **模糊題**（需求本身未收斂，怎麼做都「對」但價值不同）：不要自己收斂後悶頭做。動作 = 列出 2-3 種解讀、各自後果、你的建議，一次問 Fat Mo（R3 格式）。**由使用者收斂需求永遠比由模型猜便宜。**
2. **品味判斷**（UI 美感、文案語感、「哪個方案更優雅」）：Sonnet 等級的品味判斷不可靠且無法自知。動作優先序：(a) 找可量化代理指標（點擊次數、行數、載入時間）；(b) 多答案評審——3 個獨立產出 + 按明文 rubric 互評選優（rubric 必須先寫好，事後打分=自欺）；(c) 外部第二意見（AG/Gemini 視角，或升 opus/fable）；(d) 都不行就明說「此判斷超出可靠範圍，建議 Fat Mo 目測定奪」。**明說做不到 > 假裝有品味。**

- ✅ **正例**：S124 前端遇成本未隨件數累加，只做誠實警示（qtyWarn），**拒絕做 185×N 假乘法**——不知道真值時，暴露不知道，比編一個看起來合理的數字高一個文明等級。
- ❌ **反例**：把「IG 訊息文案改自然一點」自行定義成「加 emoji」然後宣告完成——模糊題被偷偷收斂成自己會做的題。正解是先問清「自然」對操作者意味著什麼（對客人的語氣？長度？格式？）。
